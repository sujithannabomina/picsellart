{
  "name": "picsellart-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "lint": "echo \"(lint skipped)\"",
    "build": "echo \"(no build step)\"",
    "deploy": "firebase deploy --only functions:onPublicImageUploaded"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1",
    "picsellart": "file:.."
  }
}
